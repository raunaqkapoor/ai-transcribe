{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run index.ts",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/index.ts",
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Run standupPrepare.ts",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/standupPrepare.ts",
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}"
    }
  ]
}
