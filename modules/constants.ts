export const INPUT_DIRECTORY = './inputFiles' as const
export const PROCESSING_DIRECTORY = './processingFiles' as const
export const OUTPUT_DIRECTORY = './outputFiles' as const
export const STANDUP_OUTPUT_DIRECTORY = './outputFiles' as const
/** Linear team keys to include in standup (e.g. ['Product']). Modify to match your workspace. */
export const LINEAR_TEAM_KEYS = ['Product'] as const
